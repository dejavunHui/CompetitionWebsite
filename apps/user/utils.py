from .models import User
from django.contrib.auth import login


class Utils:

	@staticmethod
	def login(self,request,user):
		request
		user.is_active = True

	def backOut(self,request,user):
		user.is_active = false